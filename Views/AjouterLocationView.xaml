<UserControl x:Class="kami_heim.Views.AjouterLocationView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:kami_heim.Views"
        mc:Ignorable="d"
        Height="360" Width="600">
    <Grid Background="{DynamicResource MaterialDesignPaper}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,10">
            <TextBlock Text="Bien :" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding Biens}" 
              SelectedItem="{Binding BienSelectionne}" 
              DisplayMemberPath="Adresse"
              Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10">
            <TextBlock Text="Locataire :" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <ComboBox ItemsSource="{Binding Locataires}" 
              SelectedItem="{Binding LocataireSelectionne}" 
              DisplayMemberPath="Nom"
              Width="200"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10">
            <TextBlock Text="Date début :" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <DatePicker SelectedDate="{Binding DateDebut}" Width="200"/>
        </StackPanel>

        <Button Content="Créer la location" Grid.Row="3" Margin="0,20,0,0"
        Command="{Binding AjouterCommand}" Width="150"/>
    </Grid>
</UserControl>
